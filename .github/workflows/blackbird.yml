name: Blackbird

on:
  pull_request:
    branches: [main, master]
  push:
    branches: [main, master]

jobs:
  fly:
    name: Fly Blackbird
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: "1.24"

      - name: Fly Blackbird
        uses: ./
